name: assasment1_ppbl
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  charts_flutter: ^0.12.0
  community_charts_flutter: ^1.0.2
  flutter:
    sdk: flutter
  flutter_charts: ^0.5.2
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true